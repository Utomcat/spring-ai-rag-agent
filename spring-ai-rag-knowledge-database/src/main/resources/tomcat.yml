server:
  # Tomcat配置（Spring Boot 4.x已默认启用虚拟线程）
  tomcat:
    threads:
      # 最大工作线程数（虚拟线程场景可适当调大）
      max: 200
      # 最小空闲线程数
      min-spare: 10
    # 连接超时时间（20秒）
    connection-timeout: 20s
    # 等待队列长度
    accept-count: 100