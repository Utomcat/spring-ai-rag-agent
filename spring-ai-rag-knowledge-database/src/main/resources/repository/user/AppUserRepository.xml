<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ranyk.spring.ai.rag.knowledge.database.repository.user.AppUserRepository">
    <resultMap id="BaseResultMap" type="com.ranyk.spring.ai.rag.knowledge.database.domain.user.entity.AppUser">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="real_name" jdbcType="VARCHAR" property="realName"/>
        <result column="avatar" jdbcType="VARCHAR" property="avatar"/>
        <result column="role" jdbcType="VARCHAR" property="role"/>
        <result column="status" jdbcType="TINYINT" property="status"/>
        <result column="create_by" jdbcType="BIGINT" property="createBy"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_by" jdbcType="BIGINT" property="updateBy"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
        <result column="last_login_time" jdbcType="TIMESTAMP" property="lastLoginTime"/>
    </resultMap>

    <!-- 根据用户名查询用户信息 -->
    <select id="selectUserByUsername" resultMap="BaseResultMap">
        select id,
               username,
               password,
               real_name,
               avatar,
               role,
               status,
               create_by,
               create_time,
               update_by,
               update_time,
               last_login_time
        from t_user
        where username = #{username}
    </select>

    <select id="countUserRegByDayLast7" resultType="map">
        SELECT DATE_FORMAT(DATE(create_time), '%Y-%m-%d') AS dayKey, COUNT(*) AS cnt
        FROM t_user
        WHERE create_time >= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY DATE_FORMAT(DATE(create_time), '%Y-%m-%d')
        ORDER BY dayKey
    </select>
</mapper>